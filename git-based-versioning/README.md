A library for computing version numbers based on Git commit hash and commit number.

This library was extracted from `mps-gradle-plugin`. It is both used during the build of the plugin and "re-exported" as
its API dependency.
